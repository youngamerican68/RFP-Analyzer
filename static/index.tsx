/**
 * Frontend entry point for AI PR Risk Summary
 *
 * This is a Custom UI Forge app that renders in Jira issue panels.
 */

import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
